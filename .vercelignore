# Files excluded from the BACKEND Vercel deployment.
# This file applies when the backend project Root Directory = "." (repo root).
.venv
tests
uploads
node_modules
frontend
# Exclude the old serverless function directory (entrypoint is now main.py)
# NOTE: do NOT put "api" here — that also matches app/api/ which contains the routes
/api
