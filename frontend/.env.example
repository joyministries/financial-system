# Used only at BUILD time on Vercel (and locally if you set it).
# Points the React app at your hosted FastAPI backend.
# Leave unset to use same-origin '/api/v1' (dev proxy / reverse proxy).
VITE_API_BASE=https://api.yourdomain.com/api/v1
