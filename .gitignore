__pycache__/
*.py[cod]
*.so
.env
.venv/
venv/
*.egg-info/
dist/
build/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Uploaded application documents (birth certificates, transcripts, ...)
uploads/

# Local development / session artifacts (not part of the product)
PROGRESS.txt
financial_see.txt
celerybeat-schedule

# Local env reference files (contain secrets — never commit)
env.txt
frontend-vercel-env.txt

# Frontend
frontend/node_modules/
frontend/dist/
frontend/tsconfig.tsbuildinfo

# Markdown docs — only the root README is tracked
*.md
!/README.md
.env.production
env.txt
