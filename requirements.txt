# Vercel Python runtime installs from requirements.txt (pyproject.toml is ignored).
# Mirrors pyproject runtime deps MINUS the container-only pieces:
#   - gunicorn / uvicorn: serverless, not long-running servers
#   - mangum: not needed — Vercel natively supports ASGI, no Lambda adapter required
# celery + redis are included so app/tasks imports don't crash at bundle time;
# the actual worker/beat never runs on Vercel (Vercel Cron calls HTTP endpoints instead).
# Keep in sync with pyproject.toml [project.dependencies].
fastapi>=0.111.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
pydantic[email]>=2.7.0
pydantic-settings>=2.3.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0
python-multipart>=0.0.9
httpx>=0.27.0
slowapi>=0.1.9
reportlab>=4.0.0
celery[redis]>=5.4.0
redis>=5.0.0
